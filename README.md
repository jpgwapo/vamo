#### Setup and run the APP

+ Setup the env file
+ composer install
+ npm install
+ npm run dev
+ docker-compose build
+ docker-compose up -d
+ Run the migration
+ docker exec container_id php artisan migrate